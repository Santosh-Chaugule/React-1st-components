import React, { Component } from 'react'
import G from './G'

export default class F extends Component {
    render() {
        return (
            <React.Fragment>
                <h1>We need much less than we think we need.</h1>
                <G />
            </React.Fragment>
        )
    }
}
